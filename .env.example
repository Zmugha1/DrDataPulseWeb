# Copy to .env locally. Never commit real keys.
VITE_SUPABASE_URL=https://blnjfadgbkjpqinsdctm.supabase.co
VITE_SUPABASE_ANON_KEY=

# Server-only (Netlify Functions / local scripts). Never prefix with VITE_.
FRED_API_KEY=

# Also set FRED_API_KEY in the Netlify dashboard env vars before deploy.
